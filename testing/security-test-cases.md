# Security Test Cases

Test suites include authentication bypass, session fixation/revocation, account enumeration, object-level authorization, privilege escalation, mass assignment, injection, upload handling, SSRF, rate limits, webhook authenticity/replay, secret leakage, and audit completeness.

High-risk features add abuse-specific tests. Tests validate both rejection and safe, non-disclosing error responses.
