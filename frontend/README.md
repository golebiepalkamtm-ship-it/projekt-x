# UX and Frontend Bible

Frontend implementation follows product contracts. Each screen specifies purpose, actor, permissions, states, accessibility, analytics, performance, error recovery, and links to APIs/events. Components are accessible by default and never enforce security decisions client-side.
