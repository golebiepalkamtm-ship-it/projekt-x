# Design System Contract

The design system owns tokens, typography, colour roles, spacing, elevation, motion, icon usage, responsive breakpoints, and component semantics. Tokens encode intent such as `surface-danger` rather than a raw colour value.

Components document variants, states, keyboard operation, focus treatment, screen-reader labels, motion reduction, loading, error, and localisation behaviour. Product teams compose components; they do not fork hidden interaction rules.
