# AI Monitoring and Rollback

Every deployment emits model/version, policy version, latency, availability, input/output safety counters, reviewer disagreement, and outcome metrics. Drift and harm thresholds create an owned incident, pause, or rollback action.

Rollback restores a known model/configuration and preserves evidence for investigation. A kill switch disables AI assistance while leaving a deterministic non-AI workflow available.
