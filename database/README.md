# Database Bible

This directory defines data ownership and behaviour before ORM schemas. A schema change is acceptable only when the affected entity, lifecycle, invariant, access policy, retention, events, indexes, migration, and operational plan are documented.

The documents describe logical design. Physical storage and provider choices remain implementation decisions.
